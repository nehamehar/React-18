

function App() {
  const [color, setcolor] = useState("red") //as use of state reflect on screen //color is current value of state//setcolor allow you to update the state// as we passesd usestate (0), so the color initial value is 0
  return (
    <>
    <div className="w-f"></div>
    </>
  )
}

export default App
